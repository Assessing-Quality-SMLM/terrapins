When the Thunderstorm checkbox is unticked a menu will appear 

![CSV parsing options](./guide/images/csv_parsing.png)

This lets you tell the parse where various fields are located. The x and y fields must be filled in and the others are optional if your fitter does not support them. To make the parse ignore a field enter a -1. Sigma refers to the uncertainty value of a fit. 